# Changes log — 2026-08-11 session

## Crash reporting: Sentry → Firebase Crashlytics (verification pass)

The actual migration (package.json, `src/services/crashReporter.ts` rewrite,
`index.js`, `jest.setup.js` mock, `android/build.gradle` +
`android/app/build.gradle` Crashlytics plugin wiring, CI's Sentry
release-metadata step removed, `docs/SENTRY.md` → `docs/CRASHLYTICS.md`) was
already done before this pass started. What was missing and is now fixed:

- **`npm install` had never been run** against the edited `package.json` —
  `@react-native-firebase/crashlytics` wasn't actually in `node_modules` yet,
  and `@sentry/react-native` was still there as a stale leftover. Ran it:
  6 packages added, 13 removed, `package-lock.json` now consistent.
- Verified no other Sentry references remain anywhere in the repo except
  expected historical ones (`changes.md`'s old entries, `crashReporter.ts`'s
  "migrated from Sentry" comment, `docs/CRASHLYTICS.md`'s own migration note).
- Verified `ErrorBoundary.tsx` needed no changes — it only calls
  `captureError` from `crashReporter.ts`'s abstraction, which is exactly
  the point of that abstraction.
- Confirmed the backend's own error tracking (GlitchTip, via
  `app/core/config.py` / `app/main.py` in `ustadAppBackend`) is a separate,
  unrelated system and was correctly left untouched — Crashlytics has no
  backend/Python equivalent, so there was never a "backend side" to this
  migration.

## Verified

- `npx tsc --noEmit` — clean.
- `npx jest --ci --no-coverage --maxWorkers=2` — 5 suites, 19 tests, all pass.

## Still open

- Nothing has been pushed — this and the 2026-08-10 analytics work are all
  still uncommitted on `Ahmad/dev` in both repos.
- Firebase console → project `ustadapp-c21a0` → Crashlytics tab should be
  opened once after the first real build ships, to confirm collection is
  live (Firebase sometimes needs that first visit to fully activate a new
  integration).
- iOS Firebase linking / EAS → TestFlight pipeline: still blocked on Apple
  Developer Program enrollment + App Store Connect credentials, unrelated to
  this migration.
