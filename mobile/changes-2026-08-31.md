# Changes — 2026-08-31

## LessonSessionScreen.tsx — comment only, no functional change

Updated the catch-block comment around `submitLockRef`/`answeredExIdsRef`
reset (near the end of `submitAnswer`) to note that a resubmitted `ex_id` is
now safe server-side: the backend's `process_answer()` (see
`ustadAppBackend/CHANGES.md`, 2026-08-31) stores each grading result keyed by
`ex_id` and replays it on a repeat instead of falling through to the generic
mismatch fallback, which previously risked grading a retried submit against
whatever the user happened to tap on the resend rather than the original
answer. No app code path changed — this documents a guarantee the frontend's
existing defensive reset already relied on being true.

## Build: 1.0.25 / 26083121, AAB

Built from `C:\BuildProjects\ustadapp-mobile\`. Backend left on **testing**
(explicit choice, not production) — artifact is not suitable for a real Play
Store submission as-is. See `versions.md` for full verification notes
(native-file diff, bundle freshness check, aapt2 version confirmation).

Output: `C:\BuildProjects\ustadapp-mobile\ustadapp-1.0.25-26083121.aab`
