# Changes 2026-09-14

## Finalized and shipped v1.0.29 release notes, triggered a new production iOS build

`mobile/src/data/releaseNotes.ts` had an uncommitted, unreviewed draft
sitting in the working tree from a prior session — the v1.0.29 entry only
listed 3 of the fixes actually present in the last shipped build (`e26e181`,
submitted 2026-09-13 as iOS build 10035). Showed the full drafted diff to
the user for approval (per the standing release-notes review rule) before
touching anything; approved as-is.

**Committed** (`d9e2bfd`): expanded the v1.0.29 entry to the complete list —
feedback sound on iPhone, map recommended-node hijack fix, fill-in-blank
text clipping, recitation audio bleeding into the next attempt, hold-to-replay
answer audio, level time estimates, and disabling Try Again/Next while audio
plays — and bumped the entry's date to 2026-09-13 to match when those fixes
actually shipped.

Pushed to `Ahmad/dev`, then triggered `ios-testflight.yml` via `gh workflow
run` (run `34845965116`). Full pipeline succeeded in 16m3s — build, submit,
and Apple's TestFlight processing all green. Independently confirmed via
`eas-cli build:list`: build `8dd99574-d90b-4234-a95e-dd14603d25f8`, iOS build
number **10036**, commit `d9e2bfd` (exact match), status `finished`, real
`.ipa` artifact.

No native or JS logic changed this session — this build's only difference
from 10035 is the release-notes text now visible in the in-app version popup.

Also noticed while logging this: `versions.md` had never once recorded an
iOS TestFlight build before today (only Android APK/AAB) — added an entry
for 10036 to start closing that gap, did not backfill 10034/10035.
